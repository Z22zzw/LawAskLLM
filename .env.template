# ============================================================
# 法律 LLM 平台 — 环境变量模板
# 复制此文件为 .env 并填入真实值
# ============================================================

# ── LLM（OpenAI 兼容接口）──
LLM_API_KEY=在此填写你的 API Key
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=deepseek-v3.2

# ── DashScope Embedding ──
DASHSCOPE_API_KEY=在此填写你的 DashScope Key

# ── MySQL ──
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=lawllm2026
MYSQL_DB=law_llm

# ── JWT（生产环境请替换为随机长字符串）──
JWT_SECRET_KEY=请替换为安全的随机字符串

# ── 检索参数（可选）──
RETRIEVAL_TOP_K=6
RETRIEVAL_USE_MMR=false
RETRIEVAL_MMR_FETCH_K=24
RETRIEVAL_MMR_LAMBDA=0.55

# ── 后端 CORS（生产环境填写你的域名）──
# ALLOWED_ORIGINS=["https://yourdomain.com"]
